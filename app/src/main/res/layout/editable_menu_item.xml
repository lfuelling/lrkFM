<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/menu_item_action_edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:background="@color/transparent"
        android:src="@drawable/ic_mode_edit_black_24dp"
        android:contentDescription="@string/a11y_desc_edit" />

    <ImageButton
        android:id="@+id/menu_item_action_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:background="@color/transparent"
        android:layout_toEndOf="@id/menu_item_action_edit"
        android:src="@drawable/ic_delete_black_24dp"
        android:contentDescription="@string/a11y_desc_delete" />

</RelativeLayout>